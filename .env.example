# MLflow AI Gateway Environment Variables
# Copy this file to .env and fill in your actual API key

OPENAI_API_KEY=your_key_here

# For production: Use Docker secrets or Vault instead of .env file
# Example with Docker secrets:
# docker secret create openai_api_key <(echo -n "your-actual-key")
# Then reference in docker-compose.yml as:
# secrets:
#   - openai_api_key
# environment:
#   OPENAI_API_KEY_FILE: /run/secrets/openai_api_key
